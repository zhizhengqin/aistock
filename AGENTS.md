 # AGENTS.md — 睿见投研项目 AI 协作规范
 
 > 本文件是本项目所有 AI 编程助手（Codex、Codex CLI 等）必须遵守的协作纪律。
 > 人类项目负责人也应阅读，了解 AI 会按什么流程做事、在哪一步需要你确认。
 
 ## 一、一口气干完再汇报
 
 - 一个任务的所有步骤在同一个回合里连续做完。
 - 计划一经确认就自主执行到底，步骤之间不需要再问我。
 - 只有两种情况可以中途停：① 遇到真正需要你决策的岔路口（说明选项和推荐）；② 彻底卡死、自己解决不了（说明试过什么）。
 - 每个回合结束前自查：这个任务是否已 100% 完成并验证？没完成就继续干，不要用"下一步可以……"收尾。
 - 长命令（超过 2 分钟）主动检查状态并告诉我进展，但不要因此中断任务。
 
 ## 二、双框架开发纪律：gstack 管流程，Superpowers 管纪律
 
 做任何新功能或改动迭代时，必须严格按以下流程，不许跳步直接写代码：
 
 1. **流程阶段（gstack）**：`/office-hours` 或 `/spec` 明确需求 → `/plan-eng-review` 出工程方案 → 我确认后才进入开发 → 完成后 `/review` 审查 → `/qa` 实测页面 → 全部通过才 `/ship` 或提交推送。
 2. **编码纪律（Superpowers）**：Build 阶段先写测试（红）再实现（绿）；TDD、YAGNI（不做没要求的功能）、完成前必须验证；一个 bug 一个原子提交。
 3. **硬性关卡**：测试全绿才能进 `/review`；`/review` 和 `/qa` 都通过才能推送部署。
 4. **小改动**（单行修复、文案调整）可以走轻量路径，但也必须：先找根因不许猜 → 改完跑相关测试 → 验证页面无回归。
 5. **每个阶段开始时告诉我现在处于哪个阶段、这个阶段要产出什么。**
